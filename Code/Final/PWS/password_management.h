// Define the SSID for the WiFi connection (network name hidden for privacy)
#define SECRET_SSID "********"                    

// Define the password for the WiFi connection (password hidden for privacy)
#define SECRET_PASS "********"                

// Define the username for the MQTT broker (user identity hidden for privacy)
#define SECRET_MQTTUSER "********"

// Define the password for the MQTT broker (password hidden for privacy)
#define SECRET_MQTTPASS "********"

// Define the server URL for the MQTT broker (server address hidden for privacy)
#define SECRET_MQTTSERVER "********"

// Define the port number for the MQTT broker connection (using default MQTT port)
#define SECRET_MQTTPORT 1884
